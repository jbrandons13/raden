services:
  - type: web
    name: raden-erp
    env: docker
    region: singapore
    plan: starter
    branch: main
    dockerCommand: "node server.js"
    envVars:
      - key: NEXT_PUBLIC_SUPABASE_URL
        sync: false
      - key: NEXT_PUBLIC_SUPABASE_ANON_KEY
        sync: false
